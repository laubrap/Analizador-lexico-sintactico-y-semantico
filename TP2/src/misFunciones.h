#include "structsTP2.h"

nodo * agregarALista(nodo* , char* );
int estaEnLista(nodo* , char* );
int cantidadDeVecesQueAparece(nodo* , char*);
int obtenerValorDeOctal(int );
int obtenerValorDeHexadecimal(char* );